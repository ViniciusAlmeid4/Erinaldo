programa {
  funcao inicio() {
    real lado, total

    escreva("Digite um lado de um prisma: ")
    leia(total)

    escreva("Digite outro lado de um prisma: ")
    leia(lado)

    total = total*lado

    escreva("Digite outro lado de um prisma: ")
    leia(lado)

    total = total*lado

    escreva("\nO volume do prisma e: ", total)
  }
}
